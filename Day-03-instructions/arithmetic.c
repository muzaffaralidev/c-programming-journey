#include <stdio.h>
#include <math.h>

int main(void){
    // int a = 1, b = 3 ; 
    // int sum = a + b;
    // int multiply = a * b;

    // int x , y = 2 * 2; x main value save nhi hogi
    // printf("%d",y);
    
    // Valid
    // int a , b , c; 
    // a = b + c;
    // b = 2 * a;
    // c = a / b;


    // Invalid
    // int x , y, z;
    // x + y = z; error
    // a = bc
    // a = b^c  == ^ zor operater perfom in bit inside

    // int a ,b , c ; 
    // a = 2;
    // b = 3;
    // c = 4;
    // a = pow(b,c);
    // printf("%d",a);


    //  Moduler Operator %
    // return reminder for int 
    printf("%d \n", 14%6); // 2 is reminder 
    printf("%d ", -14 % 6); // numireter is negative so output is also negative
    
    return 0 ;



}